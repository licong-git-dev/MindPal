# MindPal Frontend Dockerfile
FROM nginx:alpine

# 删除默认nginx配置
RUN rm /etc/nginx/conf.d/default.conf

# 复制nginx配置
COPY nginx.conf /etc/nginx/conf.d/

# 复制前端文件
COPY frontend /usr/share/nginx/html

# 暴露端口
EXPOSE 80

# 启动nginx
CMD ["nginx", "-g", "daemon off;"]
